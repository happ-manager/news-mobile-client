import type { FC } from "react";
import React from "react";
import { StyleSheet, Text, View } from "react-native";

const App: FC = () => (
	<View>
		<Text>Happ</Text>
	</View>
);

export default App;
